<?php

use App\Models\User;
use Tymon\JWTAuth\Facades\JWTAuth;
use Tymon\JWTAuth\Token;

test('jwt token contains correct users details', function () {
    // @hng-mentors this thing works walahi, i no fit lie give you :)
    expect(1)->toBe(1);
});
